import { QuoteRequest } from "../../strategyV2/model";

describe("Empty tests", () => {
  describe("Test", () => {
    it("test", async () => {
      const req: QuoteRequest | null = null;
      console.log("Testing");
    });
  });
});
