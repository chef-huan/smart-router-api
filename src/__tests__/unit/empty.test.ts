import { QuoteRequest } from "../../model";

describe("Empty tests", () => {
  describe("Test", () => {
    it("test", async () => {
      const req: QuoteRequest | null = null;
      console.log("Testing");
    });
  });
});
